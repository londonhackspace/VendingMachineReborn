language: cpp

cache:
  directories:
    - toolchain

# no point building master for now
branches:
  except:
  - master

install: ./getToolchain.sh

before_script:
  - mkdir build
  - cd build
  - cmake ..  -DCMAKE_TOOLCHAIN_FILE=../targets/tivac-launchpad/EK-TM4C1294XL.cmake

script: make